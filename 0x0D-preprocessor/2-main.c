#include <stdio.h>
#include <stdlib.h>

/**
 * main - write a program that peint the name of the file
 * it was compiled from,
 * followed by a new line.
 * Return: Always 0
 */

int main()
{
	char ch = '2-main.c';

		_putchar(ch);
	_putchar('\n');
	return(0);
}
