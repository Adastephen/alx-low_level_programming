#include "main.h"

/**
 * _isdigit - main check the idgit
 *
 * @c: check if is a digit
 * Return: 1 else 0
 */
int _isdigit(int c)
{
	if (c > 47 &&  C < 58)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
