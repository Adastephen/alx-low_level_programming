#include "main.h"

/**
 * _isupper - main entry
 *
 * @c: check the uppercase
 * Return: 1 else 0
 */
int _isupper(int c)
{
	int (char c);

	if (c == 'A')
	{	_putchar(c);
		_putchar("\n");

		return (1);
	}
	else
	{
		return (0);
	}
}

